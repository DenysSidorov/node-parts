extends layout

block content

  h1 Welcome, #{user.displayName}. Your email: #{user.email}.

  p.greeting You are logged in.

  form(action="/logout" method="post")
    button.btn.btn-primary(type="submit") Logout
